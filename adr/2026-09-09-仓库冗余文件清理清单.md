---
title: 仓库冗余文件清理清单
sidebar_label: 仓库冗余文件清理清单
---

# 仓库冗余文件清理清单

生成日期：2026-09-09
扫描范围：`docs/`、`blog/`、`static/`
状态：**已执行**（2026-09-09 完成清理，详见「八、执行记录」）

## 一、汇总

| 类别 | 文件数 | 体积 | 处理建议 |
| --- | --- | --- | --- |
| 可安全移除（官方源可还原） | 5 | 84.61 MB | 移除，链接已补录 |
| 建议保留（官方源已失效） | 1 | 3.81 MB | 保留，不可再获取 |
| 著作权风险文件 | 5 | 5.97 MB | 建议移除，改留读书笔记 |
| 系统冗余文件 | 1 | — | 移除并加入忽略规则 |
| **合计可回收** | **12** | **约 94.4 MB** |  |

## 二、可安全移除：官方源可还原的安装包

已下载官方同名包并完成 MD5 比对，**本地文件与官方发行包逐字节一致**，移除后可完整还原。

| 文件 | 体积 | 官方下载地址 | MD5 |
| --- | --- | --- | --- |
| `docs/kubernetes/app/skywalking/java/k8s/inject/skywalking-swck/skywalking-swck-0.9.0-bin.tgz` | 34.37 MB | https://archive.apache.org/dist/skywalking/swck/0.9.0/skywalking-swck-0.9.0-bin.tgz | `548cf3a09829bf0d32c564c7b31986ae` |
| `docs/docker/dev_utls/dev-container/remote-ssh/frp/p2p/frp_0.62.1_linux_amd64.tar.gz` | 12.56 MB | https://github.com/fatedier/frp/releases/download/v0.62.1/frp_0.62.1_linux_amd64.tar.gz | `1e8e703edfa96e915d158b9f3767da3a` |
| `docs/docker/dev_utls/dev-container/remote-ssh/frp/history/frp_0.62.1_linux_amd64.tar.gz` | 12.56 MB | 同上 | 同上 |
| `docs/docker/dev_utls/dev-container/remote-ssh/frp/history/v3/frp_0.62.1_linux_amd64.tar.gz` | 12.56 MB | 同上 | 同上 |
| `docs/docker/dev_utls/dev-container/remote-ssh/frp/history/server/frp_0.62.1_linux_amd64.tar.gz` | 12.56 MB | 同上 | 同上 |

说明：

1. 四份 frp 包 MD5 完全相同，为同一文件的重复副本，实际内容仅 12.56 MB；
2. SkyWalking SWCK 在 GitHub Releases 上的直链已返回 404，需改用 Apache 归档地址；
3. 下载地址与校验值已写入 `docs/kubernetes/app/skywalking/java/k8s/inject/inject.md` 与 `docs/docker/dev_utls/dev-container/remote-ssh/frp/history/doc.md`。

### 移除前需处理的依赖

`docs/` 下共 11 个 Dockerfile 通过 `COPY ./frp_0.62.1_linux_amd64.tar.gz` 引用该包，其中仅 4 个目录实际存放了该包，其余 7 个为既有坏链：

| 状态 | 目录 |
| --- | --- |
| 有包 | `frp/p2p`、`frp/history`、`frp/history/v3`、`frp/history/server` |
| 缺包（现存坏链） | `frp/history/v4`、`frp/history/v5`、`frp/history/v6`、`frp/history/tcpmux`、`frp/history/server/v2/build-image`、`frp/history/server/v4/build-image`、`frp/history/server/v4/build-image/tcpmux` |

移除本地包之前，需将上述 `COPY` 指令统一改为下载方式。11 个 Dockerfile 中已有 3 个保留了对应的 `wget` 注释，可直接启用。

## 三、建议保留：官方源已失效

| 文件 | 体积 | 说明 |
| --- | --- | --- |
| `docs/tools/softs/clash/history/clash-linux-amd64-v1.18.0.gz` | 3.81 MB | 原版仓库 `Dreamacro/clash` 已于 2023 年归档，v1.18.0 的官方下载地址现返回 404 |

该文件为当前可获取的副本，移除后无法从公开渠道还原，建议保留。后继项目 mihomo（原 Clash Meta）维持相同版本号序列且配置兼容，下载地址已补录至 `docs/tools/softs/clash/history/clash.md`。

## 四、著作权风险文件

`docs/books/` 下 5 个文件均标注来源为 `Anna's Archive`，属未经授权传播的受著作权保护作品，且随站点公开发布。

| 文件 | 体积 |
| --- | --- |
| 富爸爸穷爸爸（20周年修订版）.mobi | 2.56 MB |
| 小狗钱钱（套装全2册）.epub | 1.32 MB |
| 失落的百年致富圣经.mobi | 1.05 MB |
| 蛤蟆先生去看心理医生.mobi | 0.87 MB |
| # 心灵成长之旅：《蛤蟆先生去看心理医生》快速阅读.pdf | 0.17 MB |

建议移除实体文件，改以书目信息与读书笔记形式保留。`docs/books/` 下当前无任何 `.md` 文档，移除后该目录将为空。

## 五、系统冗余文件

| 文件 | 处理建议 |
| --- | --- |
| `docs/.DS_Store` | 移除，并在 `.gitignore` 中追加 `.DS_Store` |

## 六、已完成的改动

### 6.1 下载链接补录

1. `docs/kubernetes/app/skywalking/java/k8s/inject/inject.md` — 新增「安装包获取」章节，含 Apache 归档地址、镜像选择页、下载命令与 MD5 校验值；
2. `docs/docker/dev_utls/dev-container/remote-ssh/frp/history/doc.md` — 新增「安装包获取」章节，含官方下载地址、MD5 校验值及 Dockerfile 下载式写法；
3. `docs/tools/softs/clash/history/clash.md` — 新增「内核下载源」章节，标注原版已归档、本地备份为唯一副本，并补录 mihomo 下载地址。

### 6.2 Dockerfile 改造

11 个 Dockerfile 中的 `COPY ./frp_0.62.1_linux_amd64.tar.gz` 已统一替换为下载式指令，原先缺包的 7 处坏链随之修复：

```dockerfile
RUN wget -O ./frp_0.62.1_linux_amd64.tar.gz https://github.com/fatedier/frp/releases/download/v0.62.1/frp_0.62.1_linux_amd64.tar.gz && \
    tar -xzf ./frp_0.62.1_linux_amd64.tar.gz && \
    mv frp_0.62.1_linux_amd64 frp && \
    rm -rf ./frp_0.62.1_linux_amd64.tar.gz
```

改写由 `scripts/rewrite-frp-dockerfile.py` 完成，该脚本支持 `--dry-run` 预演。经核对，11 个镜像的基础层均已安装 `curl` 与 `wget`，下载指令可直接生效。

### 6.3 文件移除

共移除 11 个文件，均采用 `git rm`（Git 历史可恢复），执行前已备份至 `/tmp/doc-record-backup-20260909`：

- SkyWalking SWCK 发行包 1 个（34.37 MB）
- frp 发行包 4 个（合计 50.24 MB）
- `docs/books/` 下电子书实体文件 5 个（合计 5.97 MB）
- `docs/.DS_Store` 1 个

`docs/` 目录体积由 105 MB 降至 14 MB。Clash 发行包按第三节结论予以保留。

### 6.4 书目信息留存

`docs/books/doc.md` 记录了已移除书籍的书名、作者、出版年与原格式，替代实体文件保留书目元数据。

### 6.5 校验结果

- 全仓无残留 `COPY ./frp_0.62.1` 指令；
- 11 个 Dockerfile 各含 1 处下载指令；
- 无 Markdown 链接指向已删除文件；
- `docs/.DS_Store` 已清零，`.gitignore` 已包含 `.DS_Store` 与 `**/.DS_Store`。

## 七、注意事项

### Git 历史体积

上述大文件已进入提交历史（最早可见于「迁移hexo框架到docsify」提交）。仅在工作区删除文件不会减小仓库体积，历史对象仍会被克隆。

如需真正瘦身，需重写历史，例如：

```bash
git filter-repo --path <文件路径> --invert-paths
```

该操作会改写提交哈希，需协调所有协作者重新克隆，建议在执行前备份。

### 跨端构建关联

本站正在进行跨端改造（参见 `2026-09-09-跨端方案调研.md`），小程序端构建脚本须显式排除二进制文件，当前 `docs/` 中的约 94 MB 二进制资源不应进入小程序包。

## 八、执行记录与后续事项

### 已完成

1. 移除四份 frp 包与 SkyWalking SWCK 包（合计 84.61 MB）；
2. 改造 11 个 Dockerfile 的 `COPY` 指令为下载式，修复 7 处既有坏链；
3. 移除 `docs/books/` 下 5 个电子书文件，改以 `docs/books/doc.md` 留存书目；
4. 清理 `docs/.DS_Store`。

### 待办

1. **提交变更**：文件移除与 Dockerfile 改造已通过 `git rm` 暂存，需执行提交后方才生效；
2. **构建验证**：当前环境未安装 `node_modules`，未执行 `pnpm build`。提交前建议构建一次，确认无坏链告警；
3. **Git 历史瘦身**：如需回收仓库体积，需重写历史（见第七节），该操作会改写提交哈希，须协调协作者重新克隆；
4. **备份处置**：`/tmp/doc-record-backup-20260909`（53 MB）为临时备份，确认无需回滚后可删除。
