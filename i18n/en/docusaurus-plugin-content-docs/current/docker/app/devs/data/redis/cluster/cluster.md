# Employment Records

## todo

- Best if this configuration is imposed to a service name, see if it is possible
  - Related configuration file[redis.yml](./simple/v2/redis.yml)

```shell
echo 'cluster-announcement-ip 10.0.16.27' >> redis.conf &&
# echo 'cluster-announcements-ip recdis1' >> reconf &&
```
