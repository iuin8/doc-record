# Deployment Records

## todo

- Test if this configuration is adjusted to a service name, see if it is possible
  - Related configuration file[redis.yml](./simple/v2/redis.yml)

```shell
echo 'cluster-announcement-ip 10.0.16.27' >> redis.conf &&
# echo 'cluster-announcement-ip recdis1' >> redisconf &&
```
