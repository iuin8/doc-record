# cpolar+ssh+docker access restricted network

Purpose: Client environments can only be resolved using jumpserver limit
by starting a docker

- View Log

```bash
journalctl -u polar-wrapper.service
```
