# tomcat employee warp's documentation

## Missing font problem

- [对应的Dockerfile](./has-font/Dockerfile)

```bash
# Just ad a line of code in Dockerfile, which needs to be extranet when generating a project's dock:
RUN apk add --update font-dejavu fonconfig
```
