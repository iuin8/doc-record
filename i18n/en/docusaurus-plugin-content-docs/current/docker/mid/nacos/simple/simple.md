# nacos

## Quick Start

Open command window to execute：

- Clone project

  ```powershell
  git clone --depth 1 https://github.com/nacos-group/nacos-docker.git
  cd nacos-docker
  ```

- Standalone Derby

  ```powershell
  docker-compose -f example/standalone-derby.yaml up
  ```
