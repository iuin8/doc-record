# Enable ssl

## telnet 443 inoperable

- The probability is that the port mappings of the docker-compose file needs to be checked
- Use the `nettat-npl | grep 443` command to view openings
- Or use `telnet ip 443` to view openings
