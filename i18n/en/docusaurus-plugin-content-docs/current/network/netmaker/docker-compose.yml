services:
  netclient:
    image: gravitl/netclient:v0.24.1
    network_mode: Post
    privileged: true
    environment:
      - TOKEN={ENROLLMENT_KEY}
    volumes:
      - ./netclient:/etc/netclient
