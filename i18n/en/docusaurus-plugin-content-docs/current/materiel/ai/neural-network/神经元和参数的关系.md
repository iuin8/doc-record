# Rural and parameter relations

## AI FAQ

> Question: Model of shares of parameters, what is the parameter here? Is it a near in the neurote?

Response:

The parameter in the billion points model does not mean a neuron in the neuronet. The following is a specific description of：

### Meaning of model parameters

In the field of machine learning and artificiality, The parameters of the model are the variants that the model learns from data in the course of training and is used to promote the behaviour of the model to optimize predictive capability. With a simple linear regression model \(y = wx + b\) as examples, \(w\)(weight) and \(b\)(deviation) are the parameters of the model. He number and complexity of the parameters in the neurological network are more numerous and more complete, including the weight of the neurological connection and the bias of the neurology, which either determined how the neurological network processes and converts the input data to produce the corresponding output.

### Distinction from neurons

- **Concept different**：neurons are the basic components of the neurological network that receive input signatures, process them and product output signs. Armeters are the values used to describe the properties of the connection strength, deviation, etc. Between neurons. or example, in a simple pre-feedback neurological network, each need has several input connections, Each of which corresponded to a weight parameter that determines the extent to which input signatories influence the neuron activity.
- **Quantity relationships**：A neuron may have multiple parameters associated with it, but one parameter does not equate to a neuron.In the case of a neuron with \(\n\) input connections, it has \(n\) weights and \(1\) preferences with a total of \(n + 1\) parameters to describe the neuron properties.And in a neurological network with multiple neurons, the number of parameters increases dramatically as the number of neurons and the way they are connected. 比如，一个有 \(1000\) 个神经元的全连接神经网络，如果每个神经元有 \(1000\) 个输入连接，那么仅权重参数就有 \(1000\times1000 = 100\) 万个，再加上每个神经元的偏置参数，总参数数量会更多.
- **Difference**：neurons are primarily responsible for processing and transmitting information, which are non-linear changes to input signatories by activating functions,thereby model complex data. Chaired through the training process to find an optimal set of values that will allow models to better integrate training data and have good generalizations. n other words, the needs are the structural components of the model, While the parameters are adaptable elements used to optimize the performance of the model.
