# Jedis, depending on spring-data-redis, added \\`<optional>true<optional>to require developer active implication dependence.

In Spring Data Redis `jedis` relies on labeled `<optional>true</optional>`because Spring Data Redis umbrella that supports multi-Redis driven, including Jeis and Lettuce etc. His means that Spring Data Redis does not oblige projects to use Jedis, But both allow developers to choose which customers will use according to their needs. hereford, the `jedis` dependence is set to be optional, so developers need to explicitly reduce this dependence only if they need to use the Jeedis client.
With respect to the specific role of `<optional>true</optional>`, when this is marked as `true`, Maven buildings the project, If other agencies already provide implementation of this dependence, then this dependency will not be added to class. He has the advantage of reducing project dependence on conflict and volume, especially when another customer is (e). Lettuce) is already used by default in the project.
In some cases, if you explicitly refer to the Jewish class or method in your code, Even if it is optional, Maven will come back on it as compilation and running. o, if you need to use Addis Ababa in the project, you need to explicitly reduce this dependence and mark it as optional. Set up `<optional>false</optional>`) to ensure that the project is correctly built and using the Jeedis client.
Overall,<optional>true</optional>\` is marketed to provide flexibility to allow development to decide whether to reduce Addis Ababa dependency on their needs and project configuration. He correct way to introduce the Addis Ababa dependency on whether you need to use the Jeedis client in your project, and whether you have selected other Redis customers (e). Lettuce).
