input LOR
  tcp
    Import => 5044
    # codec => json_lines
    codec => json
  }
}

# input LOs
# logback
# port => 5044
# type => "logback"
# timezone => "UTC"
# timestamp_field => "@timestamp"
# timestamp_format => "yyyy-MM-d'T'H:mm:ss.SSZ"
#   }
# }

# filter LOs
# ruby
# code => "
# event.set('@timestamp', event.get('logback.timestamp'))
#     "
#   }
# }

Output
  elasticsearch LOs
    hosts => ["http://10.0.1.93:9200"]
    codec => "json"
    index => "ls-bp-%{+YYYY.MM}"
    user => "elastic"
    password => "123456"
  }
  # stdout LO/codec => rubydebug }
}
