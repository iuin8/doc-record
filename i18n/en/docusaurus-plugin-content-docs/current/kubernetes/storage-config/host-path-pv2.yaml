apiVersion: v1
kind: PersistVolume
metadata:
  name: host-path-pv
  labels:
    type: Local
spec:
  storageClassName: Standard
  capacity:
    storage: 10Gi
  accessModes:
    - ReadWriteMany
  hostPath:
    path: "/mnt/data"
