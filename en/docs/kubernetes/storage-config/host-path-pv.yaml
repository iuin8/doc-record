apiVersion: v1
kind: PersistVolume
metadata:
  name: host-path-pv
  labels:
    type: Local
spec:
  storageClassName: Standard
  capacity:
    storage: 1 Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/mnt/data"
