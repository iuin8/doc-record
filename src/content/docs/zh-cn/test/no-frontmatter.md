# 无 front matter 的文档

这篇文档故意不声明 front matter，用于验证：作者直接写 Markdown 时，
页面标题由正文首个一级标题推导，且正文中的该标题不再重复渲染。

正文的其余内容仅作占位，说明二级标题与段落的渲染不受影响。

## 二级标题

无 front matter 的文档同样参与搜索索引、侧边栏自动生成为 llms.txt 分卷，
其标题来自 loader 从正文推导的结果，与显式声明 title 的文档一致。
