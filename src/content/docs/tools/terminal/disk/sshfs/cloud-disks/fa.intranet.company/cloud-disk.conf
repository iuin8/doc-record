# 只需要配置远端 SSHFS 地址；磁盘 ID / 挂载点 / 日志名默认从 SSH host 推导。
REMOTE="fa.intranet.company:/data/nfs_share/dev"

# 可选覆盖项
SSH_PORT="22"
# IDENTITY_FILE="/Users/fa/.ssh/id_ed25519_sshfs_fa"
# EXTRA_SSHFS_OPTS=""
